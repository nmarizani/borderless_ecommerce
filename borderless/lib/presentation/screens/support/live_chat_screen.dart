import 'package:flutter/material.dart';

class LiveChatScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Live Chat")),
      body: Center(child: Text("Live Chat Feature")),
    );
  }
}
